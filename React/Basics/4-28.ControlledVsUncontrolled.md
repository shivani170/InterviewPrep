Controlled components are used when control is required over data being rendered into a form

- They are accessed through the state which makes it easier


Uncontrolled components are used when there is no need to deynamically inspect the user input