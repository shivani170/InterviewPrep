ReactPortals:
A Portal is a way to render a child component in a different location in the DOM outside the parent's component hierarchy, while maintaing react component structure.

Eg:
<Modal />
<Tooltip />
