const validAnagram = () => {
    const s = 'anagram'
    const j = 'naagram'

        const sMap =  new Map()
        const j1 = new Map()

    for( let i = 0; i < s.length; i++){
        if(sMap.has(s[i])){
            sMap

        }
        s1.set(s[i], 1)
    }
}